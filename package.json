{
  "name": "evo-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "check": "node tools/check-site.mjs ${CHECK_URL:-http://localhost:3999}",
    "check:mobile": "node tools/check-mobile.mjs ${CHECK_URL:-http://localhost:3999}",
    "check:all": "npm run check && npm run check:layout && npm run check:mobile",
    "check:layout": "node tools/check-layout.mjs ${CHECK_URL:-http://localhost:3999}"
  },
  "dependencies": {
    "next": "^15.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
